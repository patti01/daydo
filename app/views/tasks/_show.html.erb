<div id="comments">
  <% if @comments.blank? %>
    <%= render 'comments/form', task: @task, comment: @comment %>
  <% else %>
    <% @comments.each do |comment| %>
      <%= render 'comments/show', task: @task, comment: @comment %>
    <% end %>
  <% end %>
</div>


